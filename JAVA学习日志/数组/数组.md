# 格式
## 类型[] 变量名 = new 类型[n];


## 稀疏数组
### 定义一个新的二维数组用来存放原本数组中存放有值的坐标，这个新的数组第一个存放原本数组的行数和列数
### 这个数组的行数为原本数组中存有值的个数+1(第一行要用来存放原数组的行数和列数），列数固定为3列，第一列存放行坐标，第二列存放列坐标，第三列存放值
```java
//创建稀疏数组
int [][] array = new int [num+1][3];
```
### 通过遍历数组来获取原本数组中存有值的个数及将其坐标存入稀疏数组